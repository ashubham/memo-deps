{
  "name": "memo-deps",
  "version": "1.0.0",
  "description": "Memo with Dependencies, same API as React.useMemo",
  "module": "lib/index.js",
  "scripts": {
    "test": "jest --coverage",
    "posttest": "cat ./coverage/lcov.info | coveralls",
    "prepublishOnly": "npx tsc -p ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ashubham/memo-deps.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ashubham/memo-deps/issues"
  },
  "homepage": "https://github.com/ashubham/memo-deps#readme",
  "devDependencies": {
    "@types/shallowequal": "^1.1.1",
    "coveralls": "^3.1.0",
    "jest": "^26.1.0",
    "ts-jest": "^26.5.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "shallowequal": "^1.1.0"
  },
  "jest": {
    "testEnvironment": "node",
    "preset": "ts-jest",
    "testMatch": [
      "**/*.spec.ts"
    ]
  }
}